export default function TeamPage() {
  return (
    <div style={{ maxWidth:500, margin:'60px auto', textAlign:'center', color:'var(--text-muted)', fontSize:13 }}>
      <h2 style={{ fontSize:16, fontWeight:700, color:'var(--text-primary)', marginBottom:8 }}>Team</h2>
      <p>Team features coming when you connect a database.</p>
    </div>
  )
}
